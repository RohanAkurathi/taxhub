{
  "name": "taxhub",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered tax platform prototype where every number on a return traces back to its source document",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "setup": "npm install && npm run dev",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "preview": "next build && next start",
    "check": "tsc --noEmit && eslint src",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.11",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.11",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
